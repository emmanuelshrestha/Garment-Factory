{
  "name": "garment-factory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "start": "node src/main.ts",
    "migrate": "node src/db/migrate.ts",
    "test": "node --test tests/unit tests/integration",
    "test:watch": "node --test --watch tests/unit tests/integration",
    "seed": "node scripts/seed.ts",
    "backup": "node scripts/backup.ts",
    "verify-ledger": "node scripts/verify-ledger.ts"
  },
  "dependencies": {},
  "devDependencies": {}
}
